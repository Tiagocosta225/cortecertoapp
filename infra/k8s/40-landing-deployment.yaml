apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortecertoapp-landing
  namespace: cortecerto
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cortecertoapp-landing
  template:
    metadata:
      labels:
        app: cortecertoapp-landing
    spec:
      containers:
        - name: landing
          image: ghcr.io/your-org/cortecertoapp-landing:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
